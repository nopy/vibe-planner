apiVersion: v1
kind: Service
metadata:
  name: opencode-controller
  namespace: opencode
  labels:
    app: opencode
    component: controller
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 8090
    protocol: TCP
  selector:
    app: opencode
    component: controller
