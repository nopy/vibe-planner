apiVersion: v1
kind: Service
metadata:
  name: opencode-controller
  namespace: opencode
  labels:
    app: opencode
    component: controller
spec:
  type: ClusterIP
  selector:
    app: opencode
    component: controller
  ports:
  - name: http
    port: 8090
    targetPort: 8090
    protocol: TCP
