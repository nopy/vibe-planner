apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: opencode

resources:
  - namespace.yaml
  - rbac.yaml
  - configmap.yaml
  - secrets.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app: opencode
